package iuh.fit.adapter;

public class JsonToXmlAdapter {
    public String convertXmlToJson(String xmlData) {
        // Convert XML -> JSON (giả lập)
        System.out.println("Convert XML -> JSON");

        return "{ \"data\": \"" + xmlData + "\" }";
    }
}
